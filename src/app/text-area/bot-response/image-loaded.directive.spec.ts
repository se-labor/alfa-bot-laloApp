import {ImageLoadedDirective} from './image-loaded.directive';

describe('ImageLoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLoadedDirective();
    expect(directive).toBeTruthy();
  });
});
